services:
  web-profil:
    build:
      context: ./web-profil-sekolah
    container_name: profil-sman1ketapang
    restart: always
    env_file: ./web-profil-sekolah/.env
    ports:
      - "3000:3000"
    volumes:
      - ./web-profil-sekolah/prisma:/app/prisma
